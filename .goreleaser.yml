builds:
  -
    ldflags:
      - -s -w -extldflags "-static" -X main.build={{.Version}}

    env:
      - CGO_ENABLED=0

    goos:
      - linux
      - freebsd
      - windows
      - darwin

    goarch:
      - arm
      - 386
      - amd64
      - arm64